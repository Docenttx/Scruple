schema_version = "1.0.0"

id = "scruple_blender"
version = "0.1.0"
name = "Scruple"
tagline = "Provenance, C2PA, and chain-lock for Blender"
maintainer = "Docent LLC <scruple@docentechs.com>"
type = "add-on"

website = "https://scruple.ai"

tags = ["Render", "Pipeline"]

blender_version_min = "4.2.0"

license = ["SPDX:LicenseRef-Proprietary"]

copyright = [
  "2026 Docent LLC (dba Docent Technologies)",
]

platforms = ["windows-x64", "macos-arm64", "macos-x64", "linux-x64"]

permissions.network = "Send hashed provenance records to scruple.ai and receive lock receipts"
permissions.files = "Read output files after render/save/export to compute hashes"

wheels = []
