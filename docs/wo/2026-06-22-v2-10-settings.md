# WO-10 · Settings surface restructure

**Scope:** Reorganize `/settings` into four sections per the v2 spec.

**Reference:** `docs/architecture/canvas-v2.md` decision 10.

## Sections

```
Settings → Payment
  Default for one-shot charges (chain locks, machine builds):
    ◉ Stripe (fiat)  ○ RVN wallet (non-custodial)
  Canvas sessions: Stripe required, billed per-second
    [Manage Stripe card]   (existing Stripe Elements wallet card UI)

Settings → Provenance Add-ons
  ☐ Custom Machine — your own pinned node manifest, hash committed to every iteration
       (Charge per image build via your PaymentMode above. When off: shared default-scruple-canvas-v1.)
  ☐ Triple-chain lock by default — RVN + IPFS + Arweave (vs RVN-only basic)
       (Override per-lock at the lock dialog.)

Settings → Compute
  Default machine for canvas sessions:
    ◉ T4 ($0.59/hr — default)
    ○ A10G ($1.10/hr)
    ○ A100 ($3.09/hr)
    ○ H100-CC ($4.56/hr — L1+L2+L3 trust tier, TEE attested)
  (Pre-auth per session = 1h × picked machine rate; capture = actual usage.)

Settings → Receipt Publication
  Default mode for new iterations:
    ◉ Full  ○ Hash-only  ○ Witness-only
  (Override per iteration in the workspace.)
```

## Files

- `app/settings/page.tsx` — restructure into the four sections (tabs or stacked sections)
- `components/settings/PaymentSection.tsx` — NEW (or extend existing)
- `components/settings/AddOnsSection.tsx` — NEW
- `components/settings/ComputeSection.tsx` — keep, post-WO-2 cleanup
- `components/settings/PublicationSection.tsx` — NEW (WO-9 already builds this; this WO just links it in)

## Verify

- `/settings` renders the four sections in order
- Toggling Custom Machine ON kicks off WO-7 build flow + payment intent
- Compute picker still works (WO-2's clean ComputeSection)
- Payment section shows current card; "Manage Stripe card" opens portal or inline editor

## Out of scope

- Manifest editor UI (WO-11)
- Test net banner / dev guard (already in place from prior work)
