# WO-12 · Receipt UI updates — machine block + publication redaction + issuer icon

**Scope:** Make receipts surface the new v2.2 fields and respect publication mode.

**Reference:** `docs/architecture/canvas-v2.md` decisions 5, 6, 8; build plan step 18.

## Files

- `app/receipt/[scrId]/page.tsx` — already redacts per WO-9; extend:
  - Add "Machine" block under existing hash blocks: machine label, manifest hash (link to manifest diff page), key custom-node packs and commit shas
  - Add issuer-mode icon next to chain lock SCR-ID: "Scruple-issued" (Stripe-pivot) vs "User-issued" (RVN-pivot)
- `app/receipt/[scrId]/manifest/page.tsx` — NEW; renders the manifest_json for that iteration; shows diff vs prior version if user owned a previous machine

## Verify

- `/receipt/<v2.2-iteration>` shows Machine block + issuer icon
- `/receipt/<v2.1-iteration>` (legacy) shows "Machine info: unavailable for legacy iteration"
- Publication mode redaction still active

## Out of scope

- Public sharing UX (existing share buttons unchanged)
