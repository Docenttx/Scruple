# WO-2 · Strip user-tier concept

**Scope:** scruple-web is paid-only; tiers are gone. Anyone signed in with a Stripe card on file can pick any GPU. No Free / Pro / Premium / Enterprise distinctions.

**Reference:** `docs/architecture/canvas-v2.md` decision 1.

## Files

- DELETE: `lib/compute/userPlan.ts`
- DELETE: `components/CanvasLauncher.tsx`
- EDIT: `lib/compute/machines.ts` — drop `allowedPlans`, `tierLabel`, `DEFAULT_MACHINE_BY_PLAN`, `getMachineCatalogForPlan`, `getDefaultMachineForPlan`. Keep id/label/gpu/hourlyRate/description.
- EDIT: `lib/compute/getActiveMachine.ts` — drop plan validation; first-use default = `t4-free` (rename to `t4` since "free" no longer applies)
- EDIT: `app/api/settings/compute/route.ts` — drop tier-403 path
- EDIT: `app/api/canvas/session/route.ts` — drop tier-403 path (will be rebuilt fully in WO-6, but strip the tier check now)
- EDIT: `components/settings/ComputeSection.tsx` — drop Free/Pro/Premium chrome; show all 4 machines
- EDIT: `app/canvas/page.tsx` — drop CanvasLauncher import + Free-CTA branch (Canvas page rewritten in WO-5; minimal touch here)
- EDIT: `.env.local` — comment out `SCRUPLE_ENTERPRISE_EMAILS` (kept in history; vestigial)
- EDIT: any imports of deleted symbols → update

## Renames

- Machine ids: `t4-free` → `t4`; `a10g-pro` → `a10g`; `a100-premium` → `a100`; `h100cc-enterprise` → `h100cc`
- Update migration 019's default seed if needed (it's a column-add migration so the seed is set at row-create time, not in migration)

## Verify

- `rg "allowedPlans|tierLabel|userPlan|SCRUPLE_PRO_EMAILS|SCRUPLE_ENTERPRISE_EMAILS" lib/ app/ components/` → empty
- `npx next build` (or `npx tsc --noEmit`) → green
- Manual: `/settings` → Compute section renders all 4 machines without tier badges

## Out of scope

- Stripe gating (WO-6 adds the "has card on file" check)
- Removing tier-related copy from receipt UI (WO-12)
