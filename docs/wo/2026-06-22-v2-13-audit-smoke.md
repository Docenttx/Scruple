# WO-13 · Audit script v2.2 + final smoke

**Scope:** `scripts/audit-receipts.py` handles both v2.1 and v2.2. Run a full smoke pass to confirm 100% audit green pre-deploy.

**Reference:** `docs/architecture/canvas-v2.md` build plan steps 19, 20.

## Files

- `scripts/audit-receipts.py` — dispatch on `iteration.leaf_scheme`:
  - `'v2.1'` → existing logic
  - `'v2.2'` → include `machine_manifest_hash` in preimage canonicalization
- `CHANGELOG.md` — append entry for canvas v2 build complete
- `docs/sessions/2026-06-22.md` — session report (this WO's final commit)
- `memory/MEMORY.md` (auto-mem) — pointer at v2 build complete + next-session takeaway

## Verify

- Run audit over every existing iteration row: 100% green
- Spawn 3 new iterations on default machine → all v2.2, all audit green
- Spawn 1 iteration on a custom machine → v2.2 with distinct manifest_hash; audit green
- Stop receiver, kill ws-proxy, restart: all sessions resume cleanly

## Out of scope

- Production deploy itself (user-run after green)
