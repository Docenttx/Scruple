# Session: 2026-06-08 — Canonical docs + 7-layer verification

Picked up the v2.1 codebase from the 2026-05-22 session. This session was
about turning the working knowledge into durable, trustworthy documentation
that survives compaction — and proving it actually matches the code.

## What happened

### Stale-state cleanup
- Dev server was down; relaunched on `:3001` via `setsid` background launch.
- User reported sign-in button "did nothing." Server log showed zero POST
  hits → client-side issue. Root cause: stale `.next` cache in the
  browser. Cleared `.next/`, hard-restarted, told user to hard-refresh.
  (No code change required.)

### Documentation pass — two complementary docs

Authored two docs that complement each other rather than overlap:

1. **`docs/CANONICAL_GUIDE.md`** — by-component reference (commit `3d4a159`).
   System map, repo layout, per-component descriptions (web, witness
   server, Modal runner, external anchors), auth/identity model, lock
   state machine, Stripe integration, common ops with working commands,
   known gotchas, files-never-to-touch, priority-ordered follow-ups,
   quick-reference card.

2. **`docs/END_TO_END_FLOW.md`** — linear narrative trace (commit `faf5cdd`).
   Six phases tracing one user journey through every system in the order
   it happens:
   - Phase 0 static deployment setup
   - Phase 1 sign in (NextAuth + whitelist)
   - Phase 2 project + storage provider setup
   - Phase 3 capture one iteration (every hash, every external call)
   - Phase 4 lock pipeline (Stripe → witness → RVN/IPFS/Arweave)
   - Phase 5 the receipt
   - Phase 6 third-party verification flow
   Plus three appendices: ASCII swim-lane sequence diagram; cross-reference
   table mapping every hash to where it's computed and stored on each
   side; one concrete real example with live numbers.

Cross-linked them (commit `16f27af`) and updated the memory pointer.

### Self-audit — 7 layers, found 10 drifts

User asked "did you compare your document to what the code actually has
written in?" Honest answer: no — I'd written from working memory. Did a
systematic verification by layer:

| Layer | What | Drifts found |
|---|---|---|
| L1 | Every file path I cited | 0 (all exist) |
| L2 | DB schemas (iterations, projects, witnesses, locked_projects, storage_providers) | 3 — wrong table name, missing tables on witness side, undocumented extra columns on `locked_projects` |
| L3 | Witness server endpoint list | 1 — listed 7 of 14 endpoints |
| L4 | Code primitives (canonicalRecord field order, MODEL_LOADERS, deriveScrId, sha256Hex, hashPair, resolveInput, ingestIteration hash order, inputCanonical formula) | 3 — chain SCR-ID fallback chain, SCRB_ prefix, lock-sig "action in tuple" overgeneralized |
| L5 | Commands (audit script, session-token query) | 1 — "331/331" claim was a snapshot, not durable (logs expire) |
| L6 | Phase narratives | 1 — storage provider table was `storage_providers`, not `storage_settings` |
| L7 | Cross-reference table + Appendix C concrete example | 2 — Appendix C had wrong project's numbers; missed `iterations.previous_hash` vs `witnesses.prev_record_hash` distinction |

Honest scoreboard: **18 high-stakes claims verified accurate**, **10 drifts
caught and fixed** across two commits (`6c2de82`, `88ed603`).

The big lesson encoded in both docs now: write from code, not memory.
When asked to verify, actually verify.

### Audit script limitation surfaced

Re-ran `scripts/audit-receipts.py` cold: 286/331. All 45 failures are
log-presence checks ([WITNESS] / [CHECKPOINT] / [LOCAL_LOCK] / [ANCHOR])
where the witness journal `--since '6 hours ago'` window had rolled past
the original session entries. **Data-integrity checks (~286) are still
at 100%**. The audit script mixes durable data checks with ephemeral log
checks and reports one number — needs to split. Added as Open Follow-up
#0 in CANONICAL_GUIDE.

### Reusable verification prompt

User asked for a prompt that another Claude Code could use to do the same
canonical-docs + verification work on a different application. Wrote a
domain-agnostic prompt covering intent / method / outcome / mindset.
Saved as `docs/PROMPT_canonical_docs.md` so it's reusable for any future
codebase.

## Files / commits this session

```
6f2f87e (this session report)
88ed603 docs: 7-layer verification pass — fix 7 more drifts; honest audit-pass framing
6c2de82 docs: verify against code — fix three drifts in END_TO_END + CANONICAL_GUIDE
16f27af docs: cross-link CANONICAL_GUIDE → END_TO_END_FLOW
faf5cdd docs: END_TO_END_FLOW.md — linear trace from browser to RVN
3d4a159 docs: CANONICAL_GUIDE.md — single-source orientation for fresh dev/Claude
```

New files:
- `docs/CANONICAL_GUIDE.md`
- `docs/END_TO_END_FLOW.md`
- `docs/sessions/2026-06-08.md` (this file)
- `docs/PROMPT_canonical_docs.md` (reusable verification prompt)

No code changes — pure documentation + memory work.

## State of the world at end of session

- **Branch:** `feature/pivot`, head this commit.
- **Leaf protocol:** v2.1 (unchanged from 2026-05-23).
- **Audit (data band):** 286/286 (durable, repeatable).
- **Audit (log band):** ephemeral; current 0/45 because the original
  session's logs aged out of the journalctl window. Not a regression.
- **Dev server:** running on `:3001`, public URL `https://scruple.stooges.ai`.
  Witness server running on `:5799`.
- **Documentation:** byte-accurate for every layer I verified; all known
  drift surfaced and fixed.

## Open follow-ups (priority order, carried forward + new)

0. **(NEW) Audit script log/data split** — `scripts/audit-receipts.py`
   mixes durable data-integrity checks with ephemeral log-presence
   checks. Re-runs after the journalctl window expires show false
   failures. Either split the score into two reported totals (data
   vs log) or persist the witness journal + dev-server log to durable
   files.
1. **M-2: hash-on-upload** for `fetch_to_volume`. Closes the upload-
   time bait window. Pair with canonical-hash registry.
2. **Volume hygiene** — investigate non-canonical SD1.5 hash on volume.
3. **Desktop reconciliation** — v2.x leaf scheme is web-only.
4. **TEE attestation (T1/T2)** — H100 CC deploy.
5. **Login gate tiers** — single allowed-emails list today.
6. **Per-user volume namespacing** — paired with M-2.
7. **Periodic batch anchor (T6)** — pre-lock external floor.
8. **Lock-signature unification** — four sites use four different
   tuples; consider unifying so the "action in tuple → no replay"
   property holds system-wide instead of just on one path. Surfaced
   during the verification pass.

## Pickup instructions for the next session

Read in this order:
1. `memory/project_scruple_web_status.md` (one-page status + pointers)
2. `docs/CANONICAL_GUIDE.md` (~5 min, by-component reference)
3. `docs/END_TO_END_FLOW.md` (~10 min, narrative trace) — only if
   the work involves changing the provenance pipeline or external
   anchors
4. This session report — only if you need the diagnostic history

The docs are **load-bearing** as of this session — they were verified
against the code in seven layers. Trust them. If anything has drifted
since, re-run `python3 scripts/audit-receipts.py` (data band) and
spot-check the layer most relevant to your change.
