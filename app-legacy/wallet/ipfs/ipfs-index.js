/**
 * ipfs-index.js - IPFS Module Exports
 * 
 * SCRUPLE Studio V3 - AI Provenance Middleware
 * Patent Pending
 */

const { IPFSConfigManager } = require('./ipfs-config');

module.exports = {
  IPFSConfigManager
};
