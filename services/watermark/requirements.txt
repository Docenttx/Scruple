# Scruple watermark reference implementation dependencies.
#
# Pinned to specific patch versions for reproducibility.
# Dependabot updates weekly per .github/dependabot.yml.

numpy==1.26.4
pillow==10.4.0
reedsolo==1.7.0
scipy==1.18.0
